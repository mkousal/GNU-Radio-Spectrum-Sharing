id: spectrumSharing_energyDetector
label: energyDetector
category: '[spectrumSharing]'

templates:
  imports: from gnuradio import gr, spectrumSharing
  make: spectrumSharing.energyDetector(
          FFTSize=${FFTSize},
          numSubbands=${numSubbands},
          threshold=${threshold}
        )
  callbacks:
    - set_numSubbands(${numSubbands})
    - set_threshold(${threshold})

#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#     * default
parameters:
  - id: FFTSize
    label: FFT Size
    dtype: int
    default: 4096

  - id: numSubbands
    label: Number of Subbands
    dtype: int
    default: 64

  - id: threshold
    label: Threshold
    dtype: float
    default: 0.5

#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
inputs:
  - label: in
    domain: stream
    dtype: complex
    vlen: ${FFTSize}
  - label: detect_ctrl
    domain: stream
    dtype: byte

outputs:
  - label: out
    domain: stream
    dtype: float
    vlen: ${FFTSize}

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
